# Docker Compose override for development workflows.
#
# Auto-merge behaviour:
#   - This file is merged with `docker-compose.yml` only when a Makefile
#     target passes it explicitly (`-f docker-compose.dev.yml`); it is the
#     contributor convenience layer Ã¢â‚¬â€ `make dev`, `make cli-dev`, the
#     benchmark harness pick it up via the COMPOSE_DEV/COMPOSE_WATCH vars.
#   - The OSS installer (`scripts/install.sh`) does NOT download this
#     file, so end users running the launcher (`aegis-core start`) get
#     only the base `docker-compose.yml` from `~/.aegis-core/`.
#
# Skills now ship as package data under `aegis-core/skills/` and are baked
# into the LANGGRAPH image via `COPY aegis-core/` (resolved in-process by
# `aegis-core/backends/__init__.py:SKILLS_LOCAL_PATH`). On a repo dev clone
# (`make dev`) skill edits are hot-reloaded by the `./aegis-core` sync in
# `docker-compose.watch.yml` Ã¢â‚¬â€ no dedicated `./skills` bind is needed (the
# old root `./skills/` directory no longer exists).
#
# Where to put YOUR overrides depends on which stack you are running:
#   - **Launcher / OSS install** (`aegis-core start`): drop a
#     `docker-compose.override.yml` into `~/.aegis-core/`.
#   - **Repo dev clone** (`make dev`): add langgraph overrides below.
#
# See also: docs/proposals/156-compose-architecture.md.

services:
  # No langgraph overrides currently needed Ã¢â‚¬â€ skills hot-reload via the
  # ./aegis-core watch sync (docker-compose.watch.yml). Kept as a merge
  # target so the COMPOSE_DEV/COMPOSE_WATCH `-f` chains stay valid and
  # future dev-only overrides have a home.
  langgraph: {}
